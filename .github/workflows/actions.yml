name: Workflow
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
jobs:
  code-check:
    name: Check Go formatting, linting, vetting
    runs-on: ubuntu-latest
    steps:
      - name: Checkout the code
        uses: actions/checkout@v2
      - name: Run the formatter, linter, and vetter
        continue-on-error: true #! TODO(Michael: delete this line after all formatting, linting, and vetting errors have been fixed)
        uses: dell/common-github-actions/go-code-formatter-linter-vetter@main
        with:
          directories: ./...
  sanitize:
    name: Check for forbidden words
    runs-on: ubuntu-latest
    steps:
      - name: Checkout the code
        uses: actions/checkout@v2
      - name: Run the forbidden words scan
        uses: dell/common-github-actions/code-sanitizer@main
        with:
          args: /github/workspace
  test:
    name: Run Go unit tests and check package coverage
    runs-on: ubuntu-latest
    steps:
      - name: Checkout the code
        uses: actions/checkout@v2
      - name: Run unit tests and check package coverage
        continue-on-error: true #! TODO(Michael: delete this line after all unit tests errors have been fixed)
        uses: dell/common-github-actions/go-code-tester@main
        with:
          threshold: 90
  go_security_scan:
    name: Go security
    runs-on: ubuntu-latest
    steps:
      - name: Checkout the code
        uses: actions/checkout@v2
      - name: Run Go Security
        uses: securego/gosec@master
  malware_security_scan:
    name: Malware Scanner
    runs-on: ubuntu-latest
    steps:
      - name: Checkout the code
        uses: actions/checkout@v2
      - name: Run malware scan
        uses: dell/common-github-actions/malware-scanner@main
        with:
          directories: .
          options: -ri
  image_security_scan:
    name: Image Scanner
    runs-on: ubuntu-latest
    env:
      REGISTRY: localhost:5000 
      VERSION: ${{ github.sha }}
      CONTAINER_TOOL: docker
      PASSED_BUILD: false
    steps:
      - name: Checkout the code
        uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - name: Set up Go
        uses: actions/setup-go@v2
        with:
          go-version: ^1.16
      - name: Build CSM Replication Docker Images
        run: |
          make images
          if [ $? -eq 0 ]; then { echo "PASSED_BUILD=true" >> $GITHUB_ENV; } fi
      - name: Scan dell-csi-replicator Docker Image
        if: always() && env.PASSED_BUILD
        uses: Azure/container-scan@v0
        with:
          image-name: ${{ env.REGISTRY }}/dell-csi-replicator:${{ env.VERSION }}
          severity-threshold: HIGH
      - name: Scan dell-replication-controller Docker Image
        if: always() && env.PASSED_BUILD
        uses: Azure/container-scan@v0
        with:
          image-name: ${{ env.REGISTRY }}/dell-replication-controller:${{ env.VERSION }}
          severity-threshold: HIGH

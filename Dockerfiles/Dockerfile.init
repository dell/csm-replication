# Init container

# some arguments that must be supplied
ARG BASEIMAGE

FROM $BASEIMAGE AS init
WORKDIR /upgrade
COPY --chown=1001 scripts/upgrade ./
RUN chmod +x ./migrate_rg.sh
ENTRYPOINT ["/upgrade/migrate_rg.sh"]
